div(layout="column")
  //- div(layout="column", flex).scrollable
  //-   h3(ng-show="!applications.length") There is no application
  //-   md-table-container.scrollable.grids-table(layout, flex, ng-show="applications.length")
  //-     table(md-table, flex)
  //-       thead(md-head, md-order="query.order")
  //-         tr(md-row)
  //-           th(md-column) Application Name
  //-           th(md-column) Instance Name
  //-           th(md-column) Region
  //-           th(md-column) Zone
  //-           th(md-column) Create At
  //-           th(md-column)
  //-       tbody(md-body)
  //-         tr(md-row, ng-repeat="application in applications | orderBy: query.order")
  //-           td(md-cell)
  //-             span(ui-sref="user.organization.applications.application.config({applicationId: application.uuid})") {{application.name}}
  //-           td(md-cell) {{application.instanceName}}
  //-           td(md-cell) {{application.region}}
  //-           td(md-cell) {{application.zone}}
  //-           td(md-cell, style="white-space: nowrap") {{application.createdAt | date:'dd-M-yyyy'}}
  //-           td(md-cell)
  //-             a(ng-click="removeapplication(application)")
  //-               i.fa.fa-remove
  //-   md-table-pagination(md-label="{{options.label}}" md-limit="query.limit" md-limit-options="limitOptions" md-page="query.page" md-total="{{testResults.length}}" md-page-select="options.pageSelect" md-boundary-links="options.boundaryLinks" md-on-paginate="options.changePage()", ng-show="testResults.length && !isLoadingTestResults")

  div.widget-container(masonry, options="$root.masonryOptions")
    .item.widget(masonry-tile, ng-repeat="application in applications track by $index", style="width: calc(33.33% - 10px);")
      div.application
        .widget-header.bg-primary(layout, layout-align="start center")
          h4 
            strong(ui-sref="user.organization.applications.application.config({applicationId: application.uuid})") {{application.name}}
        .widget-body
          div.app-description {{application.description || 'application Description'}}
          div.app-details(layout="column")
            label
              span.label-title Created at:
              span {{application.createdAt | date:'dd-M-yyyy'}}
            label
              span.label-title Region:
              span {{application.region}}
            label
              span.label-title Zone:
              span {{application.zone}}

