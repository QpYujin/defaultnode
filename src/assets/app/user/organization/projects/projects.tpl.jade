div(layout="column", flex).scrollable
  h3(ng-show="!projects.length") There is no project
  md-table-container.scrollable.grids-table(layout, flex, ng-show="projects.length")
    table(md-table, flex)
      thead(md-head, md-order="query.order")
        tr(md-row)
          th(md-column)
          th(md-column) Project Name
          th(md-column) Stack
          th(md-column) Environment
          th(md-column) No of Apps
          th(md-column) Create At
          th(md-column)
      tbody(md-body)
        tr.tHi(md-row, ng-repeat="project in projects | orderBy: query.order")
          td(md-cell)
            span(ui-sref="user.organization.projects.project.applications({projectId: project.uuid})") 
          td(md-cell)
            span(ui-sref="user.organization.projects.project.applications({projectId: project.uuid})") {{project.name}}
          td(md-cell)
            span(ui-sref="user.organization.projects.project.applications({projectId: project.uuid})") {{project.stack}}
          td(md-cell)
            span(ui-sref="user.organization.projects.project.applications({projectId: project.uuid})") {{project.environmentDisplay}}
          td(md-cell)
            span(ui-sref="user.organization.projects.project.applications({projectId: project.uuid})") {{project.zone}}
          td(md-cell, style="white-space: nowrap")
            span(ui-sref="user.organization.projects.project.applications({projectId: project.uuid})") {{project.createdAt | date:'dd-M-yyyy'}}
          td(md-cell)
            span(ui-sref="user.organization.projects.project.applications({projectId: project.uuid})") 
              button.btn.btn-info(ui-sref="user.organization.projects.project.config({projectId: project.uuid})", ui-sref-active="hidden") 
                i.glyph-icon.tooltip-button.font-white.demo-icon.icon-gear(title='', data-original-title='.icon-gear')
                |  Config
  md-table-pagination(md-label="{{options.label}}" md-limit="query.limit" md-limit-options="limitOptions" md-page="query.page" md-total="{{testResults.length}}" md-page-select="options.pageSelect" md-boundary-links="options.boundaryLinks" md-on-paginate="options.changePage()", ng-show="testResults.length && !isLoadingTestResults")
